# Cloned app repos (separate git repos)
repos/

# Environment files with secrets
.env

# Claude config
.claude/

# Old duplicate directories (should be deleted)
backend/
frontend/
logger/
