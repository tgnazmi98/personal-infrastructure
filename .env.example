# Database Configuration
POSTGRES_DB=electrical_monitoring
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
DB_HOST=timescaledb
DB_NAME=electrical_monitoring
DB_USER=postgres
DB_PASSWORD=changeme
DB_PORT=5432

# Backend Configuration
DEBUG=0
SECRET_KEY=changeme
ALLOWED_HOSTS=api-demo.opengrid.com.my,localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=https://demo.opengrid.com.my,http://localhost:3000

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=10080

# Frontend Configuration
NEXT_PUBLIC_API_BASE_URL=https://api-demo.opengrid.com.my
NEXT_PUBLIC_WS_HOST=api-demo.opengrid.com.my

# Logger Configuration
API_BASE_URL=http://backend:8000
DEVICE_IP=192.168.68.210
DEVICE_PORT=502

# Network Configuration
HOMELAB_IP=192.168.68.100

# Domain Configuration
FRONTEND_DOMAIN=demo.opengrid.com.my
BACKEND_DOMAIN=api-demo.opengrid.com.my
JENKINS_DOMAIN=jenkins.opengrid.com.my

# Cloudflare Tunnel Configuration
# Get this from: Cloudflare Zero Trust → Networks → Tunnels → Create tunnel
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
